import { useEffect, useState } from "react";
import axios from "axios";
import toast from "react-hot-toast";
import { useNavigate } from "react-router-dom";
const API="https://humorous-fulfillment-production-1f5e.up.railway.app/api"; const auth=()=>({headers:{Authorization:`Bearer ${localStorage.getItem("token")}`}}); const field="w-full rounded-xl border border-slate-300 px-3 py-3";
export default function EmployerProfile(){const nav=useNavigate();const [f,setF]=useState({});useEffect(()=>axios.get(`${API}/employer/profile`,auth()).then(r=>setF(r.data)).catch(()=>nav("/employer/login")),[]);const change=e=>setF({...f,[e.target.name]:e.target.value});const submit=async e=>{e.preventDefault();try{await axios.put(`${API}/employer/profile`,f,auth());toast.success("Company profile saved");}catch(x){toast.error(x.response?.data?.error||"Could not save profile");}};return <main className="min-h-screen bg-slate-100 p-5"><form onSubmit={submit} className="mx-auto max-w-3xl rounded-3xl bg-white p-7 shadow-xl"><button type="button" onClick={()=>nav("/employer/dashboard")} className="font-bold text-blue-700">← Dashboard</button><h1 className="mt-4 text-3xl font-black">Company profile</h1><div className="mt-7 grid gap-4 md:grid-cols-2">{[["company_name","Company name"],["website","Website (https://...)"],["industry","Industry"],["company_size","Company size"],["company_type","Company type"],["city","City"],["state","State"],["mobile","Contact mobile"],["contact_email","Contact email"],["logo_url","Logo image URL"]].map(([name,label])=><input key={name} name={name} value={f[name]||""} onChange={change} placeholder={label} className={field}/>)}</div><textarea name="description" value={f.description||""} onChange={change} placeholder="About the company" className={`${field} mt-4 min-h-32`}/><textarea name="address" value={f.address||""} onChange={change} placeholder="Company address" className={`${field} mt-4 min-h-24`}/><button className="mt-6 rounded-xl bg-blue-600 px-6 py-3 font-bold text-white">Save profile</button></form></main>}
